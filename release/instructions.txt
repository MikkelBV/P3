P3-kalman requires the user to pick a video:
	works with videos 1-3 (use nr 1 or 3 mainly plz)

P3-bs requires the user to specify a bounding box
	make it tall and narrow around the runner and avoid including surrounding area

P3-keypoints requires the user to specify a bounding box
	make it short and fat around the runners midpoint
	make sure the box only covers the wall area (no window sill or grass plz - it will crash)
	
	